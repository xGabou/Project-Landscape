diff --git a/src/reproduction/java/baseline/reproduction/performance/ErosionExperiment.java b/src/reproduction/java/baseline/reproduction/performance/ErosionExperiment.java
index 4c3b878..a3bb67c 100644
--- a/src/reproduction/java/baseline/reproduction/performance/ErosionExperiment.java
+++ b/src/reproduction/java/baseline/reproduction/performance/ErosionExperiment.java
@@ -12,7 +12,7 @@ final class ErosionExperiment {
         var measurements=new ArrayList<Object>();long checked=0;
         for(int[] point:new int[][]{{0,0},{47,48},{-224,-96},{-64,-1172},{64,468},{-64,-3907}}){
             var input=context.cache.provide(point[0],point[1]);int size=input.getBlockSize().total();
-            var reference=Erosion.factory(context).apply(size);var candidate=Erosion.factory(context,true).apply(size);
+            var reference=Erosion.factory(context).apply(size);var candidate=ReuseErosion.factory(context).apply(size);
             for(int pass=-2;pass<7;pass++){
                 var a=input.snapshot();var b=input.snapshot();long before,referenceNs,candidateNs;
                 if((pass&1)==0){
diff --git a/src/reproduction/java/baseline/reproduction/task6b/FullChunkPerformance.java b/src/reproduction/java/baseline/reproduction/task6b/FullChunkPerformance.java
index 35ef48e..0060bdb 100644
--- a/src/reproduction/java/baseline/reproduction/task6b/FullChunkPerformance.java
+++ b/src/reproduction/java/baseline/reproduction/task6b/FullChunkPerformance.java
@@ -17,6 +17,7 @@ public final class FullChunkPerformance {
         var os=(com.sun.management.OperatingSystemMXBean)ManagementFactory.getOperatingSystemMXBean();
         var threads=(com.sun.management.ThreadMXBean)ManagementFactory.getThreadMXBean();
         try(var recording=new jdk.jfr.Recording(jdk.jfr.Configuration.getConfiguration("profile"))){
+            recording.enable("jdk.JavaMonitorEnter").withThreshold(java.time.Duration.ofMillis(1));
             recording.start();
             // Global block coordinates fixed before either version is measured. Categories are observed below.
             int[][] points={{8192,0},{8208,0},{8224,0},{8240,0},{6128,6240},{6144,6240},
@@ -52,6 +53,8 @@ public final class FullChunkPerformance {
             java.nio.file.Files.createDirectories(path.getParent());recording.dump(path);
             out.row("task6b_profiles","version",version,"path",path.toAbsolutePath().toString());out.flush();
         }
+        if(source instanceof ClimateBiomeSource)level.getChunk((6128>>4)+64,(6240>>4)+64);
+        FullOutputEvidence.run(level,out);
     }
     private static long allocated(com.sun.management.ThreadMXBean bean){long sum=0;for(long value:bean.getThreadAllocatedBytes(bean.getAllThreadIds()))if(value>0)sum+=value;return sum;}
 }
